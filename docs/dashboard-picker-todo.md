# SKIPPED: Wire Dashboard WorkerPicker to Spawn Flow

Decision: skipped — worker selection is done via config (`agent-orchestrator.yaml`) or the spawn API directly.

The WorkerPicker component and `/api/workers` endpoint remain in the codebase for future use if needed.
